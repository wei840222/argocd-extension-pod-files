apiVersion: v1
kind: Service
metadata:
  name: {{ include "argocd-extension-pod-files.fullname" . }}
  labels:
    {{- include "argocd-extension-pod-files.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "argocd-extension-pod-files.selectorLabels" . | nindent 4 }}
