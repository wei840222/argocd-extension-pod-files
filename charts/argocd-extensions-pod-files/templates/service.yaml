apiVersion: v1
kind: Service
metadata:
  name: {{ include "argocd-extensions-pod-files.fullname" . }}
  labels:
    {{- include "argocd-extensions-pod-files.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "argocd-extensions-pod-files.selectorLabels" . | nindent 4 }}
